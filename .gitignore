.venv/
__pycache__/
*.pyc
*.log
.env
.vectorstore/
.DS_Store
.git-credentials
# Test artifacts
.coverage
htmlcov/
# Jupyter
*.ipynb_checkpoints/
# Local data
sample.csv
# Development docs now included in repo for CI/CD setup reference
# development-docs/
.idea/

# Terraform local artifacts (never commit state or plans)
infra/terraform/.terraform/
infra/terraform/*.tfstate
infra/terraform/*.tfstate.backup
infra/terraform/terraform.tfstate*
infra/terraform/plan.out
infra/terraform/*.tfvars

# VSCode settings (optional, keep if intentional)
.vscode/

# Python build/test caches
.pytest_cache/
*.egg-info/
dist/
build/

# Env / secrets
.env.local
.env.*

# System files
Thumbs.db
*.swp

# Evaluation results (exclude generated files, keep queries)
eval/results*.json

# Vector store artifacts (now stored in PostgreSQL database)
artifacts/
